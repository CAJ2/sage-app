apiVersion: v1
kind: Service
metadata:
  name: {{ include "sageleaf-science.fullname" . }}
  labels: {{- include "sageleaf-science.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "sageleaf-science.selectorLabels" . | nindent 4 }}
