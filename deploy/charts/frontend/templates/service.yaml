apiVersion: v1
kind: Service
metadata:
  name: {{ include "sageleaf-frontend.fullname" . }}
  labels: {{- include "sageleaf-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "sageleaf-frontend.selectorLabels" . | nindent 4 }}
