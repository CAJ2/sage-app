apiVersion: v1
kind: Service
metadata:
  name: {{ include "sageleaf-api.fullname" . }}
  labels: {{- include "sageleaf-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "sageleaf-api.selectorLabels" . | nindent 4 }}
