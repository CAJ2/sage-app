apiVersion: v1
kind: Pod
metadata:
  name: '{{ include "sageleaf-api.fullname" . }}-test-connection'
  labels: '{{- include "sageleaf-api.labels" . | nindent 4 }}'
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sageleaf-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
